import AuthRoute from "jikan-ga-nai/framework/auth-route";

export default class Home extends AuthRoute.extend({
  // anything which *must* be merged to prototype here
}) {
  // normal class body definition here
}
